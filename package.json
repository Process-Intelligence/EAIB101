{
  "name": "eaib101",
  "version": "1.0.0",
  "private": true,
  "description": "EAIB101 student page hosting on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "build": "echo 'No build step: static assets are served from public/'",
    "index": "node scripts/build-index.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.130.0"
  }
}
